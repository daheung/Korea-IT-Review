day17(java 15일차)
복습 -> API -> Stream API -> Object 클래스 -> Wrapper 클래스
※ java 쓰레드, 파일입출력, 소켓프로그래밍 jsp 시작때 java 복습시간에 같이 진행

0. 복습
1) 람다식(Lambda)
   익명메소드, 람다식 : 메소드를 하나의 식으로 표현한다
   함수형 인터페이스 : 단 하나의 추상메소드를 가지고 있는 인터페이스
         @FunctionalInterface
   
   매개변수(x), 리턴값(x), 실행문이 1개만 있는 추상 메소드
   인터페이스 객체명 = () -> syso();

   매개변수(x), 리턴값(o), 실행문이 1개만 있는 추상 메소드
   인터페이스 객체명 = () -> "안녕";

   매개변수(o 1개), 리턴값(o), 실행문이 1개만 있는 추상메소드
   인터페이스 객체명 = 매개변수명 -> 매개변수명;

   매개변수(o 2개), 리턴값(o), 실행문이 2개 이상 있는 추상메소드
   인터페이스 객체명 = (매개변수명1, 매개변수2) -> { syso(매개변수명1 + 매개변수명2);
               return 매개변수1 + 매개변수2 };

2) 예외처리
   예외가 발생했을 때 프로그램이 비정상적으로 종료되는 것을 막고 정상적으로 실행될 수 있도록 처리하는 것을 의미

   try{

   }catch(예외클래스명 참조변수){
      예외 발생 시 실행할 코드;
   }catch(예외클래스명 참조변수){
      예외 발생 시 실행할 코드;
   }finally{
      예외발생 유무에 상관없이 무조건 실행할코드(생략가능)
   }

- 예외클래스에서 제공하는 메소드
   .getMessage()      예외 메시지를 문자열로 반환
   .toString()      발생한 예외 객체를 문자열로 반환
   .printStackTrace()   예외 발생지점을 추적하여 단계별로 모두 출력함


- throws, throw
   throw 예외 강제 발생시키기
      throw new 예외클래스명()
   throws 예외 던지기, 메소드 선언부에 사용한다 
      접근제한자 리턴타입 메소드명() throws 예외클래스명 { }

- 사용자 정의 예외클래스
   Exception, RuntimeException
   클래스명 + Exception

1. API(Application Programming Interface)
   응용 프로그래밍 인터페이스

   내부 API   
      JDK(JRE, JVM) 설치 시 제공해주는 기본 API
      자바로 어플리케이션을 쉽게 구현할 수 있도록 한 라이브러리 집합
      https://docs.oracle.com/en/java/javase/index.html

   외부 API
      다른 개발자들이 개발한 패키지 및 클래스들을 의미한다
      보통 jar(java Archive)파일로 배포하며 자바 프로젝트에 build path에 추가하여 사용할 수 있다

+) https://docs.oracle.com/en/java/javase/17/docs/api/index.html
내부 API 확인 방법

(1) 전체 상속 관계를 보려면 클래스로 들어가 위쪽의 상속 계층도를 보면된다
   String 클래스가 어떻게 정의되어있는지는 아래쪽에 있는 선언부를 보면되고
   클래스가 final인지 추상클래스인지 알 수 있고, 부모클래스/구현인터페이스 볼 수 있다
(2) 구성 멤버는 상단 메뉴의 SUMMARY를 보면된다
   멤버 별 링크를 제공하는데 링크가 있으면 공개된(public, protected) 멤버가 있다는 뜻이고 링크가 없으면 공개된 멤버가 없다는 의미
   NESTED : 중첩 클래스/중첩 인터페이스 목록으로 이동
   FIELD : 필드목록으로 이동
   CONSTR : 생성자 목록으로 이동
   METHOD : 메소드 목록으로 이동

(3) 필드 보기 : Modifier and Type에서 static여부와 필드 타입을 알 수 있고 Field와 Description은 필드명과 간단한 설명
   해당 필드 클릭 시 필드 선언부와 상세한 설명
   
(4) 메소드 보기
   ALL Methods : 모든 메소드 목록을 보여준다
   Static Methods : 정적 메소드 목록
   Instance Methods : 인스턴스 메소드 목록
   Concrete Methods : 완전한 실해부를 갖춘 메소드 목록
   Deprecated Methods : 향후 제거된 메소드 목록

1) 자바에서 기본적으로 제공하는 API 패키지 리스트
   java.lang   자바프로그래밍을 위한 가장 기본적인 패키지와 클래스를 포함
   java.util   프로그램을 제어하기 위한 클래스와 데이터를 효율적으로 저장하기 위한 클래스들 포함
         자료구조와 관련된 컬렉션 클래스를 제공
   java.io      키보드, 모니터, 프린터, 파일 등을 제어할 수 있는 클래스를 포함
         입출력 스트림 클래스를 제공
   java.net   통신을 위한 기능들(네트워크 통신과 관련된 클래스 제공)
   java.time   날짜 및 시간을 조작하거나 연산하는 클래스를 제공
   java.text   날짜 및 숫자를 원하는 형태의 문자열로 만들어주는 포맷 클래스를 제공

2) 모듈, 패키지, 라이브러리, 프레임워크
- 모듈(Module)
   가장 작은 단위
   
   jdk9부터 도입된 모듈 시스템
   코드를 논리적으로 그룹화하고 모듈간의 의존성을 관리하는 방법을 제공한다
   모듈은 패키지와 라이브러리의 개념을 확장하고 런타임에서 모듈의 논리적인 단위로 코드를 로드하고 관리할 수 있다
   코드의 의존성을 더 명확히 하고 런타임 모듈 시스템을 통해 더 격리된 환경을 제공한다

- 패키지(Package)
   관련된 클래스 및 인터페이스를 그룹화하는 방법을 제공한다
   패키지는 디렉터리 구조와 일치하고 패키지 내의 클래스들을 동일한 패키지 이름 공간안에 있어야한다
   코드를 구조화하고 이름 충돌을 방지하는데 도움을 준다

- 라이브러리(Library)
   jar(java Archive) 압축파일 형태로 존재한다
   jar 파일에는 클래스와 인터페이스의 바이트 코드(.class) 파일들이 압축되어있다

- 프레임워크(Framework)
   어플리케이션의 전체 구조와 실행 흐름을 제공하는 기반 환경
   대표적인 java 프레임워크 => Spring / Spring Boot, Struts, Hibernate(ORM 프레임워크)

=> 클래스/인터페이스 -> 패키지 -> 모듈(JDK 9+) -> 라이브러리(JAR) -> 프레임워크

3) 외부 API
   JAR 파일로 만든다
   Java Archive : java 클래스 파일, 리소스 파일 및 메타 데이터를 포함하는 아카이브 파일
   
- jar 파일 생성
   java 어플리케이션을 개발한 모든 파일들을 jar파일로 패키징

- jar 파일 배포
   jar 파일을 웹사이트, FTP 서버, 클라우드 저장소 등에 업로드 하여 배포 가능

파일 배포
   배포할 프로젝트 마우스 우클릭 > Export > Java/Jar file 선택 > Next
   > Destination 원하는 경로 선택 > Export Java Source Files ... 체크(API 주석 포함하겠다는 의미) > Finish

외부 api 추가(build path에 추가)
   배포된 jar 파일 다운로드 > 프로젝트 우클릭 > Build Path > Configure Build Path
   > Libraries 탭 클릭 > Add External JARs 클릭 > 저장된 경로의 .jar 파일 더블클릭으로 추가 > Apply 클릭
   > Orders and Exports 탭 클릭 > Select All 클릭 > Apply and Close 클릭

2. 스트림 API
   Stream API는 JAVA8에서 도입된 기능으로 컬렉션 데이터를 선언적이고 함수형 방식으로 처리하기 위한 API
   여러개의 데이터를 흐름처럼 하나씩 처리하기 위한 도구
   
   - 스트림은 데이터를 저장하지 않는다
   - 스트림은 데이터를 하나씩 흘려보내며 처리한다

   스트림 = 데이터 처리 파이프
   데이터 -> 가공 -> 결과

1) 스트림 API
   선언적 : 무엇(what)을 할 지만 표현
   
2) 스트림 연산구조
   스트림 생성 -> 중간연산 -> 최종연산

+) 컬렉션/배열과 스트림
      컬렉션/배열   스트림
데이터 저장   o      x
반복 제어    외부반복      내부반복
재사용      가능      불가
실행시점     즉시      지연

4. Wrapper Class
   기본 자료형들의 클래스 타입

   기본타입(원시타입)   wrapper 클래스(참조형타입)
   byte      Byte
   short      Short
   int      Integer
   long      Long
   double      Double
   float      Float
   boolean      Boolean
   char      Charater

1) 기본형(Primitive Type)
   첫글자 소문자
   null 초기화 불가능
   값을 그대로 저장한다

2) 참조형(Wrapper Class)
   첫글자 대문자
   Null 초기화 가능
   객체의 레퍼런스를 저장한다

+) 메모리를 더 많이 차지하는 Wrapper 클래스 언제 사용할까?
   기본형이 객체로 취급되어야하는 경우들이 있는데 이 때 Wrapper Class를 사용한다
   - 매개변수로 객체가 요구될 때
   - 기본형 값이 아닌 객체로 저장해야할 때
   - 객체간의 비교가 필요할 때

3) 박싱과 언박싱
   기본타입의 값을 포장 객체로 만드는 과정을 박싱(Boxing)이라고 하고
   포장 객체에서 기본 타입의 값을 얻어내느 과정을 언박싱(Unboxing)이라고 한다

4) boxing
- 예전 방법
   클래스타입 객체 = new 클래스타입(일반타입의 값);

- 최근 방법
   클래스타입 참조변수 = 클래스타입.valueOf(일반타입의 값);

- 자동 boxing
   클래스타입 참조변수 = 일반타입의값;

5) unboxing
   일반타입 변수 = 참조변수.000Value();

- 자동 unboxing
   일반타입 변수 = 참조변수;

5. String 메소드
   charAt()   문자열에서 해당 인덱스 번호의 문자를 char타입으로 반환
         "hello".charAt(0)   h
   length()   문자열의 길이 반환
   indexOf()   특정 문자나 문자열을 앞에서부터 찾아 처음으로 찾게되는 인덱스를 반환
         없을경우 -1을 반환
   split()      구분문자를 기준으로 문자열을 잘라 배열에 담아 반환
   strip()      문자열 앞뒤 공백 제거
   substring(start, end)   시작인덱스 번호 ~ 끝 인덱스-1까지의 문자열을 반환